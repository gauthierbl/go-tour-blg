package main

import "fmt"

func main() {
	var i int
	var f float64
	var b bool
	var s string
	fmt.Println("These are the zero values (with ut explict inital values) for the following types:")
	fmt.Printf("int: %v, float64: %v, bool: %v, string: %q\n", i, f, b, s)
}
